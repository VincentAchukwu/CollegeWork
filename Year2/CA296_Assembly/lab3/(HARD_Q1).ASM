ORG C0
DB "ABRACADABRA"
DB 0

ORG 00
MOV BL,C0		; MOVE C0 TO BL
MOV CL,[BL]		; [BL] HAS WHAT [C0] CONTAINS, MOVE IT TO CL
MOV DL,5A		; "Z"
JMP LOOP
LOOP:
	CMP CL,41	; CHECK IF CL IS A (41)
	JZ ZOOP		; IF THERE IS, JMP TO ZOOP
	INC BL		; INCREMENT BL
	MOV CL,[BL]
	CMP CL,0	; KEEP GOING UNTIL "END OF STRING"
	JNZ LOOP	; LOOP
	JMP ENDING

ZOOP:
	MOV [BL],DL	; REPLACE LETTER IN [BL] ("A") WITH "Z"
	INC BL
	MOV CL,[BL]
	CMP CL,0
	JNZ LOOP

ENDING:
	END		;COULDN'T GET IT TO END....