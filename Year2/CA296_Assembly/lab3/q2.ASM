MOV	AL,0		; MOVE NUM INTO AL AND BL
MOV	BL,14
LOOP:
	ADD AL,BL	; ADD WHATEVER BL IS INTO AL
	DEC BL		; THEN DEC BL SINCE WE ARE CHECKING BL IS NOT ZERO(MOST RECENT OPERATION)
	JNZ LOOP	; JUMP TO LOOP WHILE SIGN NOT SET
	END

;problem is that there's an overflow...